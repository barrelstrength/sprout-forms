# Need to rebuild the Example SQL insert Query
# 
# Since we are now working with Element Types, 
# we may need to do this programatically, instead 
# of with raw SQL as we no longer control all the
# tables we're inserting data into.

