{% if currentUser.can('viewSproutFormsEntries') %}
    {% redirect 'sprout-forms/entries' %}
{% elseif currentUser.can('manageSproutFormsForms') %}
    {% redirect 'sprout-forms/forms' %}
{% else %}
    {% redirect 'sprout-forms/settings/general' %}
{% endif %}
