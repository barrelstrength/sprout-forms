{% if currentUser.can('sproutForms-viewEntries') %}
    {% redirect 'sprout-forms/entries' %}
{% elseif currentUser.can('sproutForms-editForms') %}
    {% redirect 'sprout-forms/forms' %}
{% else %}
    {% redirect 'sprout-forms/settings/general' %}
{% endif %}
