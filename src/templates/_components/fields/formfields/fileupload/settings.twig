{% extends "_components/fieldtypes/Assets/settings" %}

{# Swap out instruction text with javascript so we don't have to duplicate a ton of code in the default assets settings file just to change some minor language. #}
{% js %}
    var uploadLocationInstructionText = '{{ "Note that the subfolder path can contain variables like the Form Entry ID {id} or the Form handle {{ object.getForm().handle }}."|t('sprout-forms') }}';
    $('#types-barrelstrength-sproutforms-fields-formfields-FileUpload-single-folder-settings .instructions').text(uploadLocationInstructionText);
{% endjs %}
