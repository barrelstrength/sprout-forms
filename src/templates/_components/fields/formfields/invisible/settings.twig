{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: 'Invisible Field Value'|t('sprout-forms'),
    name: 'value',
    instructions: 'Value to set when the field is saved/sent. Craft Object Syntax is supported.'|t('sprout-forms'),
    placeholder: '{{ object.entry.id is defined ? object.entry.id : null }}',
    value: field.value
}) }}

{{ forms.lightswitchField ({
    label: "Allow edits"|t('sprout-forms'),
    instructions: "Allow admin users to edit the submitted field value."|t('sprout-forms'),
    name: 'allowEdits',
    on: field.allowEdits,
}) }}

{{ forms.lightswitchField ({
    label: "Hide submitted value"|t('sprout-forms'),
    instructions: "Don't display the submitted field value."|t('sprout-forms'),
    name: 'hideValue',
    on: field.hideValue,
}) }}
