<input type="hidden"
    {%- if id is defined %} id="{{ id }}"{% endif %}
    {%- if class is defined %} class="{{ class }}"{% endif %}
    {%- if name is defined %} name="{{ name ~ '[' ~ inputName ~ ']' }}"{% endif %}
    {%- if value is defined %} value="{{ value }}"{% endif %}>
